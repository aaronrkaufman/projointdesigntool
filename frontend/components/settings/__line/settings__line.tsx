import React, { FC } from 'react';

import styles from './settings__line.module.css';

export interface SettingsLineProps {
}

export const SettingsLine: FC<SettingsLineProps> = ({  }) => (
    <div className={styles.settings__line}>{}</div>
);

