import React, { FC } from 'react';

import styles from './preview__ranking.module.css';

export interface PreviewRankingProps {
}

export const PreviewRanking: FC<PreviewRankingProps> = ({  }) => (
    <div className={styles.preview__ranking}>{}</div>
);

