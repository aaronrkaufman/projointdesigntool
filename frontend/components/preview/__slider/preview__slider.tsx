import React, { FC } from 'react';

import styles from './preview__slider.module.css';

export interface PreviewSliderProps {
}

export const PreviewSlider: FC<PreviewSliderProps> = ({  }) => (
    <div className={styles.preview__slider}>{}</div>
);

